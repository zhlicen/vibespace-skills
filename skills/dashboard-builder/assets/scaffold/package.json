{
  "name": "dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Local dynamic dashboard generated by dashboard-builder",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "xlsx": "^0.18.5"
  },
  "comment_optional_db_drivers": "If the data source is a database, also install ONE of: 'pg' (PostgreSQL) or 'mysql2' (MySQL). Both are pure-JS, no compiler needed.",
  "optionalDependencies": {}
}
